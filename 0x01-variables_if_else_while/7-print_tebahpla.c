#include <stdio.h>

/**
 * main - printing the alphabet in reverse order
 * Return: Always 0
 */
int main(void)
{
	char lb;

	for (lb = 'z'; lb >= 'a'; lb--)
		putchar(le);

	putchar('\n');

	return (0);
}
